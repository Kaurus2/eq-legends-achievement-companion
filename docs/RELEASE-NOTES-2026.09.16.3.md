## v2026.09.16.3

- First-run setup guides new users through choosing the game folder, entering /log on, exporting achievements and selecting their character.
- Each file turns green when detected. A separate message confirms new combat-log activity.
- Setup includes the annotated export screenshot and uses the app's light/dark blue-and-gold theme.
- Start tracking saves the selected profile and enables live updates. Setup stays dismissed after completion; reopen it with Setup guide in Settings.
- When multiple characters are discovered, the app now asks you to select one instead of showing misleading missing-file indicators.

Existing configured users are not forced through setup. Your saved data and mob-name lists remain in place.